name: "readout-dummy-root"
roles:
  - name: "readout-role-0"
    constraints:
      - attribute: machine_id
        value: alio2-cr1-flp134
    task:
      load: readout

  - name: "readout-role-1"
    constraints:
      - attribute: machine_id
        value: alio2-cr1-flp136
    task:
      load: readout

  - name: "readout-role-2"
    constraints:
      - attribute: machine_id
        value: alio2-cr1-flp137
    task:
      load: readout
